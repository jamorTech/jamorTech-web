// useSearch.js
import { useState } from 'react';
import { Store } from '../store/Store';

const useSearch = (searchUrl) => {

  const search = async (params) => {
    

    try {

    } catch (error) {

    } finally {
      
    }
  };

  return { search, searchLoading, searchError, searchResults };
};

export default useSearch;
